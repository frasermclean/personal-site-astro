---
import logo from '@/assets/site-logo.png';
import { Image } from 'astro:assets';

interface Props {
  size?: number;
}

const { size = 128 } = Astro.props;
---

<Image src={logo} alt="Site Logo" class="rounded-full" width={size} height={size} />
