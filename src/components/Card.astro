---
interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<section class="p-4 rounded-2xl bg-neutral-100 dark:bg-neutral-600">
  {title && <h2 class="text-2xl mb-4">{title}</h2>}
  <slot />
</section>
