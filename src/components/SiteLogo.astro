---
import { Image } from 'astro:assets';
import imageFile from '../assets/site-logo.png';

interface Props {
  size?: number;
}

const { size = 128 } = Astro.props;
---

<Image src={imageFile} alt="Site Logo" class="rounded-full" width={size} height={size} />
